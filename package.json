{
  "scripts": {
    "build": "node build.js",
    "preview": "node build.js && node --run serve",
    "serve": "npx --yes serve .deploy -p 4173",
    "deploy": "node build.js && pnpm dlx wrangler@latest pages deploy .deploy --project-name liangwenfeng"
  }
}
